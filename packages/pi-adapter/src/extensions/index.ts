// @pikit/pi-adapter/extensions: Pi's extension API as pikit supports it (SPEC §6.2b). A project
// aliases `@earendil-works/pi-coding-agent` to `@pikit/pi-extension-shim`, which re-exports this
// module, so existing Pi extensions import from it unmodified.

export * from "./api.ts";
