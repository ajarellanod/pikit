{
  "name": "pikit-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.4.0",
    "node": ">=22"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@pikit/core": "workspace:*",
    "@pikit/pi-adapter": "workspace:*",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
